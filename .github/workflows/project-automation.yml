# SPDX-FileCopyrightText: 2025-2026 SecPal
# SPDX-License-Identifier: CC0-1.0

name: Project Board Automation

on:
  issues:
    types:
      - opened
      - reopened
      - closed
      - labeled
  pull_request:
    types:
      - opened
      - ready_for_review
      - closed
      - converted_to_draft
      - labeled
  pull_request_review:
    types:
      - submitted

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  project-automation:
    name: Project automation
    # yamllint disable-line rule:line-length
    uses: SecPal/.github/.github/workflows/project-automation-core.yml@1d40667551b9288550d3e26832c9a80caea97b67 # main
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}

  draft-reminder:
    name: Draft PR Reminder
    # yamllint disable-line rule:line-length
    uses: SecPal/.github/.github/workflows/draft-pr-reminder.yml@1d40667551b9288550d3e26832c9a80caea97b67 # main
    permissions:
      issues: write
      pull-requests: write
